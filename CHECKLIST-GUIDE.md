# BlueRise Implementation Checklist

Trang chủ quản lý 119 màn/hạng mục triển khai duy nhất trên 34 điểm truy cập prototype.

## Trạng thái

- `todo`: Chưa làm
- `in-progress`: Đang triển khai
- `done`: Hoàn thành

## Lưu trữ

Dữ liệu được lưu trong `localStorage` với khóa `bluerise-implementation-checklist-v2`. Dữ liệu không tự đồng bộ giữa trình duyệt hoặc thành viên. Hãy dùng nút Xuất JSON/Nhập JSON trên trang chủ để chuyển dữ liệu.

## Triển khai GitHub Pages

Đưa toàn bộ nội dung của file ZIP vào thư mục gốc của nhánh được GitHub Pages publish. `index.html` phải nằm ở cấp gốc.
